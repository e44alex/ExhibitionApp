<Page
    x:Class="ExhibitionApp1.PicturesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExhibitionApp1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <FlipView x:Name="ImagesFlipView" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="1355" Margin="0,0,0,0" Height="825" ItemsSource="{Binding Pictures}">
            <Image x:Name="Image" Source="{Binding Pictures.Path}"/>
        </FlipView>
        <Button x:Name="OnMainPageButton" Content="On Main Page" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Height="52" Width="140" Click="OnMainPageButton_OnClick"/>

    </Grid>
</Page>
